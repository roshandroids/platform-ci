# Weekly maintenance — intentionally thin. No full matrix rebuilds.
# Status: stub — interface frozen; implementation next.
name: maintain

on:
  workflow_call:
  schedule:
    - cron: "0 9 * * 1"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintain:
    name: Maintain (stub)
    runs-on: ubuntu-latest
    steps:
      - name: Stub
        run: |
          echo "maintain.yml stub — placeholder for future cache/docs chores."
          echo "Default: no heavy builds."
