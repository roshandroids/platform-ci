# Changelog

## Unreleased

### Added

- Phase 0 scaffold: `quality` reusable workflow
- Composite actions: `read-config`, `setup-flutter`, `setup-melos`, `github-release`
- `schema/project.schema.json` + example configs
- Stub workflows: `build`, `deploy`, `release`, `maintain`
- `self-test` workflow for example config parsing
